{
  "name": "wsproxy",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun index.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode index.ts --outfile wsproxy"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}